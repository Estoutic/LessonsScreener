{
  "name": "screener-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "chrome-types": "^0.1.327",
    "esbuild": "^0.20.0"
  },
  "dependencies": {
    "jspdf": "^4.2.0"
  }
}
